// Triangle area calculator
/**
 * Problem - 7
 * Area calculator
 */

function area (base, height){
    const calculateArea = .5 * base * height;
    return calculateArea;
}

let inputNumber = area(5,5);
console.log(`The area of triangle is: ${inputNumber}`);